import React from "react";

// TODO: add redirect if user logs out
// TODO: should have the users @username as the url
const UserHome = () => (
  <div>
    user home
  </div>
);

export default UserHome;
